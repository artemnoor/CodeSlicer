Checked-in ranking baselines contain aggregate metrics and policy versions
only. Real-corpus baselines are added only when the local corpus snapshot is
available and pinned; unavailable corpora must remain explicitly marked.
