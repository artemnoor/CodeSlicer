# Forbidden Shortcuts

Forbidden before main MVP works:

- Hardcoding `OrderRepository.save` in production resolver.
- Making Graphify mandatory.
- Adding Neo4j/Postgres/Docker/web UI.
- Adding real LLM API/internet crawler.
- Building FastAPI/Django/SQLAlchemy support before generic support pack schema.
- Hiding inferred edges without evidence.
- Returning prose support pack instead of machine-readable support pack.
- Letting MCP duplicate core logic.
