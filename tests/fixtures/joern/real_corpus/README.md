# Sanitized real-corpus regression outputs

These files are checked-in, minimal interchange baselines used to test the
distinction between a complete confirmed path and an unresolved/no-path
result. They are not Joern executions and are not vulnerability findings.

The real-corpus runner consumes user-provided local Joern output only.
