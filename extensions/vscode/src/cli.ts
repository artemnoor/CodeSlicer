import{ChildProcessWithoutNullStreams,spawn}from"node:child_process";import{basename}from"node:path";
export interface ProcessResult{command:string[];cwd:string;exitCode:number;stdout:string;stderr:string}
export function formatCommand(command:readonly string[]):string{return command.map(p=>/[\s"]/u.test(p)?JSON.stringify(p):p).join(" ")}
/** Spawn argv directly: command strings are never evaluated by a shell. */
export function runProcess(executable:string,args:readonly string[],cwd:string,timeoutMs=120_000):Promise<ProcessResult>{const command=[executable,...args];return new Promise((resolve,reject)=>{let child:ChildProcessWithoutNullStreams;try{child=spawn(executable,[...args],{cwd,shell:false,windowsHide:true})}catch(error){reject(error);return}let stdout="",stderr="";const timer=setTimeout(()=>child.kill(),timeoutMs);child.stdout.on("data",(d:Buffer)=>stdout+=d.toString());child.stderr.on("data",(d:Buffer)=>stderr+=d.toString());child.on("error",error=>{clearTimeout(timer);reject(error)});child.on("close",code=>{clearTimeout(timer);resolve({command,cwd,exitCode:code??-1,stdout,stderr})})})}
export function buildReviewArgs(workspace:string,baseRef:string):string[]{return["review",workspace,"--base",baseRef,"--max-results","10","--run-tests","none","--refresh","auto","--no-daemon","--json"]}
export function buildAnalyzeArgs(workspace:string):string[]{return["analyze",workspace,"--use-scan-plan","--no-daemon"]}
export function safeTestCommand(command:unknown):string[]|undefined{if(!Array.isArray(command)||!command.length||!command.every(x=>typeof x==="string"&&x.length>0&&!/[\0\r\n]/u.test(x)))return undefined;const argv=command as string[];const runner=basename(argv[0]).toLowerCase();return new Set(["py","py.exe","python","python.exe","pytest","pytest.exe","node","node.exe","npm","npm.cmd","npx","npx.cmd","dotnet","dotnet.exe","cargo","cargo.exe","go","go.exe"]).has(runner)?argv:undefined}
