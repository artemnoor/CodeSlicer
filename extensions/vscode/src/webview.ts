import { CockpitState, EvidenceChain, ImpactItem, TestRecommendation } from "./types";

const esc = (value: unknown): string => String(value ?? "").replace(/[&<>"']/gu, character => ({
  "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;"
}[character] as string));

const riskTone = (value: string): string => /critical|high/u.test(value) ? "danger" : /medium/u.test(value) ? "warning" : "success";
const riskLabel = (value: string): string => value === "—" ? "AWAITING REVIEW" : value.toUpperCase();
const percent = (part: number, total: number): string => total > 0 ? `${Math.round((part / total) * 100)}%` : "—";
const icon = (name: "spark" | "play" | "map" | "terminal" | "arrow" | "test" | "settings" | "refresh"): string => {
  const paths: Record<string, string> = {
    spark: "M12 2l1.9 6.1L20 10l-6.1 1.9L12 18l-1.9-6.1L4 10l6.1-1.9L12 2Z",
    play: "M8 5.5v13l10-6.5-10-6.5Z",
    map: "m3 6 6-3 6 3 6-3v15l-6 3-6-3-6 3V6Zm6-3v15m6-15v15",
    terminal: "m5 7 4 4-4 4m6 1h8",
    arrow: "m5 12h13m-5-5 5 5-5 5",
    test: "M9 3h6m-3 0v8m-5 0h10l3 8H4l3-8Z",
    settings: "M12 8a4 4 0 1 0 0 8 4 4 0 0 0 0-8Zm0-5 1.1 2.3 2.5.4 1.8-1.7 1.8 1.8-1.7 1.8.4 2.5L21 12l-2.3 1.1-.4 2.5 1.7 1.8-1.8 1.8-1.8-1.7-2.5.4L12 21l-1.1-2.3-2.5-.4-1.8 1.7-1.8-1.8 1.7-1.8-.4-2.5L3 12l2.3-1.1.4-2.5-1.7-1.8 1.8-1.8 1.8 1.7 2.5-.4L12 3Z",
    refresh: "M20 12a8 8 0 0 0-14.5-4.6M4 4v4h4m-4 4a8 8 0 0 0 14.5 4.6M20 20v-4h-4"
  };
  return `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="${paths[name]}"/></svg>`;
};

function impactRow(item: ImpactItem): string {
  return `<button type="button" class="entity-row" data-entity="${esc(item.entityId)}" data-file="${esc(item.file)}" data-line="${item.line ?? ""}" title="Open evidence for ${esc(item.label)}">
    <span class="entity-glyph">${icon("arrow")}</span>
    <span class="entity-copy"><strong>${esc(item.label)}</strong><small>${esc(item.kind)} · ${esc(item.confidence)} confidence</small></span>
    <span class="row-arrow">${icon("arrow")}</span>
  </button>`;
}

function chainRow(chain: EvidenceChain, index: number): string {
  const route = chain.nodeIds.map(node => `<span>${esc(node)}</span>`).join(`<i>${icon("arrow")}</i>`);
  return `<button type="button" class="chain-row" data-chain="${index}" aria-label="Show evidence chain ${index + 1}">
    <span class="chain-index">0${index + 1}</span><span class="chain-route">${route || "Confirmed evidence path"}</span><span class="chain-proof">${chain.evidence.length} proof</span>
  </button>`;
}

function testRow(test: TestRecommendation, index: number): string {
  return `<button type="button" class="test-row" data-test="${index}" aria-label="Run recommended test ${esc(test.file || test.symbol)}">
    <span class="test-icon">${icon("test")}</span><span class="test-copy"><strong>${esc(test.file || test.symbol)}</strong><small>${esc(test.reason)}</small></span><span class="test-confidence">${esc(test.confidence)}</span>
  </button>`;
}

export function renderCockpit(state: CockpitState): string {
  const { review, project, runtime } = state;
  const evidenceCount = review.impacts.reduce((total, impact) => total + impact.evidence.length, 0) + review.chains.reduce((total, chain) => total + chain.evidence.length, 0);
  const evidenceTargets = review.impacts.length + review.chains.length;
  const provenTargets = review.impacts.filter(impact => impact.evidence.length > 0).length + review.chains.filter(chain => chain.evidence.length > 0).length;
  const proofCoverage = percent(provenTargets, evidenceTargets);
  const reviewed = review.status === "ready";
  const impacts = review.impacts.slice(0, 5).map(impactRow).join("") || `<p class="empty">Run a review to surface the nearest affected entities.</p>`;
  const chains = review.chains.slice(0, 3).map(chainRow).join("") || `<p class="empty">No cross-file chain has been proven yet.</p>`;
  const tests = review.tests.slice(0, 4).map(testRow).join("") || `<p class="empty">Targeted tests appear after CodeSlicer can prove their relevance.</p>`;
  const notices = [...review.warnings, ...review.limitations];
  const warnings = notices.map(notice => `<li><span class="warning-mark">!</span>${esc(notice)}</li>`).join("") || `<li class="quiet"><span class="warning-mark">✓</span>No warnings reported by the current review.</li>`;
  const nodes = review.impacts.slice(0, 6).map((impact, index) => {
    const positions = [[11, 26], [58, 16], [35, 48], [60, 66], [10, 74], [43, 84]][index];
    return `<button type="button" class="map-node map-node-${index % 3}" data-entity="${esc(impact.entityId)}" data-file="${esc(impact.file)}" data-line="${impact.line ?? ""}" style="left:${positions[0]}%;top:${positions[1]}%" title="Open ${esc(impact.label)}"><span></span>${esc(impact.label.slice(0, 18))}</button>`;
  }).join("") || `<div class="map-empty"><span>${icon("map")}</span>Review a local diff to materialize the impact slice.</div>`;
  const graphState = project.graphStatus === "present" ? "GRAPH READY" : project.graphStatus === "missing" ? "GRAPH MISSING" : "GRAPH UNKNOWN";
  const graphifyStatus = project.graphifyAvailable ? "Graphify graph available in Local Hub." : "Graphify not connected.";
  const runtimeState = runtime.status === "found" ? "CONNECTED" : runtime.status === "unchecked" ? "NOT CHECKED" : runtime.status.toUpperCase();
  const signalTone = runtime.status === "found" ? "is-live" : runtime.status === "error" || runtime.status === "incompatible" ? "is-error" : "is-idle";

  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<style>
:root{color-scheme:dark;--canvas:#0a0d0b;--surface:#121714;--surface-raised:#171d19;--surface-glass:rgba(22,28,24,.86);--line:rgba(209,255,220,.13);--line-strong:rgba(209,255,220,.24);--text:#f1f6f1;--muted:#9ca8a0;--quiet:#69746c;--acid:#48f477;--acid-soft:rgba(72,244,119,.14);--violet:#b999ff;--violet-soft:rgba(185,153,255,.15);--amber:#ffbf5b;--danger:#ff6575;--radius:14px;--mono:var(--vscode-editor-font-family,ui-monospace,SFMono-Regular,Consolas,monospace);--ui:var(--vscode-font-family,Inter,ui-sans-serif,system-ui,sans-serif)}*{box-sizing:border-box}html{background:var(--canvas)}body{margin:0;min-width:0;background:radial-gradient(110% 44% at 66% -8%,rgba(72,244,119,.11),transparent 42%),radial-gradient(80% 34% at -10% 35%,rgba(185,153,255,.07),transparent 46%),var(--canvas);color:var(--text);font:13px/1.45 var(--ui)}body:before{content:"";position:fixed;inset:0;pointer-events:none;opacity:.17;background-image:linear-gradient(rgba(255,255,255,.022) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.022) 1px,transparent 1px);background-size:24px 24px;mask-image:linear-gradient(to bottom,black,transparent 82%)}main{position:relative;padding:14px 12px 24px;max-width:740px;margin:0 auto}.topbar{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:14px}.brand{display:flex;align-items:center;gap:9px;min-width:0}.brand-mark{display:grid;place-items:center;width:29px;height:29px;color:var(--acid);border:1px solid rgba(72,244,119,.55);border-radius:9px;background:linear-gradient(135deg,rgba(72,244,119,.18),rgba(72,244,119,.02));box-shadow:0 0 18px rgba(72,244,119,.16)}.brand-mark svg{width:17px;height:17px}.brand-title{margin:0;font-size:14px;line-height:1.05;letter-spacing:-.01em}.brand-sub{display:block;color:var(--muted);font:10px/1.3 var(--mono);letter-spacing:.08em;text-transform:uppercase;margin-top:3px}.signal{display:inline-flex;align-items:center;gap:6px;color:var(--muted);font:10px/1 var(--mono);letter-spacing:.08em;white-space:nowrap}.signal-dot{width:7px;height:7px;border-radius:50%;background:var(--quiet);box-shadow:0 0 0 3px rgba(156,168,160,.08)}.is-live .signal-dot{background:var(--acid);box-shadow:0 0 12px rgba(72,244,119,.86)}.is-error .signal-dot{background:var(--danger);box-shadow:0 0 12px rgba(255,101,117,.75)}.eyebrow{display:flex;align-items:center;gap:7px;color:var(--muted);font:10px/1.2 var(--mono);letter-spacing:.1em;text-transform:uppercase;margin:0 0 7px}.eyebrow:before{content:"";width:17px;height:1px;background:currentColor}.hero{position:relative;overflow:hidden;padding:16px;border:1px solid var(--line);border-radius:var(--radius);background:linear-gradient(135deg,rgba(25,32,27,.96),rgba(14,18,16,.88));box-shadow:0 16px 44px rgba(0,0,0,.22)}.hero:after{content:"";position:absolute;width:180px;height:180px;right:-70px;top:-90px;border:1px solid rgba(72,244,119,.19);border-radius:50%;box-shadow:0 0 0 20px rgba(72,244,119,.025),0 0 0 46px rgba(72,244,119,.018)}.hero h1{position:relative;margin:0;font-size:19px;letter-spacing:-.035em}.hero p{position:relative;margin:5px 0 0;max-width:52ch;color:var(--muted);font-size:12px}.diff-note{position:relative;display:flex;align-items:flex-start;gap:7px;margin-top:13px;padding-top:10px;border-top:1px solid var(--line);color:#b9c8bd;font-size:11px}.diff-note svg{flex:0 0 auto;width:14px;height:14px;color:var(--acid)}.metrics{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1px;margin-top:10px;border:1px solid var(--line);border-radius:var(--radius);overflow:hidden;background:var(--line)}.metric{min-width:0;padding:14px;background:linear-gradient(145deg,var(--surface),#101411)}.metric-label{display:block;color:var(--muted);font:10px/1.2 var(--mono);letter-spacing:.1em}.metric-value{display:block;margin:6px 0 4px;font:600 clamp(24px,8vw,38px)/1 var(--ui);letter-spacing:-.065em}.metric-detail{display:flex;align-items:center;gap:5px;color:var(--quiet);font-size:10px}.metric-detail b{color:var(--acid);font-weight:600}.metric-detail .violet{color:var(--violet)}.danger{color:var(--danger)}.warning{color:var(--amber)}.success{color:var(--acid)}.meter{display:grid;grid-template-columns:minmax(18px,1.2fr) minmax(18px,.85fr) minmax(18px,.55fr);gap:3px;margin-top:10px;height:8px;padding:1px;border:1px solid var(--line);background:#0b0e0c}.meter span{position:relative;overflow:hidden}.meter .proof{background:var(--acid)}.meter .fresh{background:var(--violet)}.meter .fallback{background:repeating-linear-gradient(90deg,#687069 0 3px,transparent 3px 6px)}.meter-labels{display:grid;grid-template-columns:1.2fr .85fr .55fr;gap:5px;margin-top:7px;color:var(--muted);font:9px/1.2 var(--mono);letter-spacing:.035em;text-transform:uppercase}.meter-labels span{display:flex;align-items:center;gap:5px;min-width:0}.key{width:7px;height:7px;flex:0 0 auto;background:var(--acid)}.key.violet{background:var(--violet)}.key.gray{background:#7e857f}.command-deck{display:flex;gap:8px;align-items:stretch;margin:10px 0}.button{display:inline-flex;align-items:center;justify-content:center;gap:7px;min-height:37px;padding:8px 10px;border:1px solid var(--line-strong);border-radius:9px;background:rgba(24,30,26,.78);color:var(--text);font:600 11px/1 var(--ui);cursor:pointer;transition:transform 160ms ease,background 160ms ease,border-color 160ms ease,box-shadow 160ms ease}.button svg{width:14px;height:14px}.button:hover{border-color:rgba(72,244,119,.64);background:#1c2920}.button:active{transform:scale(.98)}.button:focus-visible,.entity-row:focus-visible,.chain-row:focus-visible,.test-row:focus-visible,.map-node:focus-visible{outline:2px solid var(--violet);outline-offset:2px}.button.primary{flex:1;border-color:rgba(72,244,119,.6);background:linear-gradient(135deg,#1c9b49,#36dd69);color:#07130a;box-shadow:0 8px 25px rgba(39,211,93,.18)}.button.primary:hover{background:linear-gradient(135deg,#28b455,#52f07c);box-shadow:0 10px 30px rgba(39,211,93,.28)}.button.icon-only{width:37px;padding:0;flex:0 0 37px}.section{margin-top:10px;border:1px solid var(--line);border-radius:var(--radius);background:var(--surface-glass);overflow:hidden}.section-heading{display:flex;align-items:center;justify-content:space-between;gap:9px;padding:11px 12px;border-bottom:1px solid var(--line)}.section-heading h2{margin:0;color:#dce5de;font:600 10px/1.2 var(--mono);letter-spacing:.11em;text-transform:uppercase}.section-count{color:var(--acid);font:10px/1 var(--mono);letter-spacing:.05em}.section-meta{color:var(--muted);font-size:10px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.project-line{padding:11px 12px;border-bottom:1px solid var(--line)}.project-path{display:block;overflow:hidden;color:#d4dfd7;font:10px/1.5 var(--mono);text-overflow:ellipsis;white-space:nowrap}.project-status{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-top:6px;color:var(--muted);font-size:10px}.project-status b{color:var(--acid);font:10px/1 var(--mono);letter-spacing:.06em}.project-status .missing{color:var(--amber)}.runtime-line{display:flex;gap:9px;align-items:flex-start;padding:11px 12px;border-top:1px solid var(--line);background:rgba(0,0,0,.12)}.runtime-line svg{width:15px;height:15px;flex:0 0 auto;color:var(--violet)}.runtime-copy{min-width:0}.runtime-copy strong{display:block;color:#dfeae1;font-size:11px}.runtime-copy small{display:block;overflow:hidden;margin-top:2px;color:var(--muted);font:10px/1.35 var(--mono);text-overflow:ellipsis;white-space:nowrap}.map-shell{padding:10px 12px 12px}.impact-map{position:relative;min-height:205px;overflow:hidden;border:1px solid rgba(182,255,205,.12);border-radius:10px;background:radial-gradient(ellipse at 52% 48%,rgba(72,244,119,.13),transparent 23%),radial-gradient(ellipse at 20% 80%,rgba(185,153,255,.11),transparent 24%),linear-gradient(135deg,#0e1510,#121612 58%,#0d100e)}.impact-map:before{content:"";position:absolute;inset:0;opacity:.35;background-image:radial-gradient(circle,rgba(224,255,233,.45) 1px,transparent 1.5px);background-size:21px 21px;mask-image:radial-gradient(ellipse at center,black,transparent 79%)}.map-path{position:absolute;inset:0;width:100%;height:100%;opacity:.62}.map-path path{fill:none;stroke:#708176;stroke-width:1.2;stroke-dasharray:4 5}.map-path path.emphasis{stroke:var(--acid);stroke-width:1.45;filter:drop-shadow(0 0 4px rgba(72,244,119,.6))}.map-node{position:absolute;z-index:1;display:inline-flex;align-items:center;gap:5px;max-width:118px;padding:6px 7px;border:1px solid rgba(162,255,186,.28);border-radius:7px;background:rgba(18,32,21,.88);box-shadow:0 8px 18px rgba(0,0,0,.25);color:#e5f4e8;font:600 10px/1 var(--mono);white-space:nowrap;text-overflow:ellipsis;overflow:hidden;cursor:pointer;transition:transform 160ms ease,border-color 160ms ease,box-shadow 160ms ease}.map-node:hover{transform:translateY(-2px);border-color:var(--acid);box-shadow:0 10px 22px rgba(0,0,0,.32),0 0 15px rgba(72,244,119,.17)}.map-node span{display:block;width:6px;height:6px;flex:0 0 auto;border-radius:50%;background:var(--acid);box-shadow:0 0 8px var(--acid)}.map-node-1{border-color:rgba(185,153,255,.36);background:rgba(29,22,40,.86)}.map-node-1 span{background:var(--violet);box-shadow:0 0 8px var(--violet)}.map-node-2{border-color:rgba(255,191,91,.34);background:rgba(41,31,17,.87)}.map-node-2 span{background:var(--amber);box-shadow:0 0 8px var(--amber)}.map-caption{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-top:9px;color:var(--muted);font-size:10px}.legend{display:flex;gap:8px;flex-wrap:wrap}.legend span{display:flex;align-items:center;gap:4px}.legend i{width:6px;height:6px;border-radius:50%;background:var(--acid)}.legend .l-violet{background:var(--violet)}.legend .l-amber{background:var(--amber)}.details{min-height:42px;margin:10px 12px 12px;padding:9px 10px;border-left:2px solid var(--violet);border-radius:0 7px 7px 0;background:rgba(185,153,255,.08);color:#cbd3cc;font:10px/1.45 var(--mono);white-space:pre-line}.list{padding:5px 8px 8px}.entity-row,.chain-row,.test-row{width:100%;border:1px solid transparent;background:transparent;color:var(--text);text-align:left;cursor:pointer;transition:background 160ms ease,border-color 160ms ease,transform 160ms ease}.entity-row:hover,.chain-row:hover,.test-row:hover{border-color:var(--line);background:rgba(255,255,255,.027)}.entity-row{display:flex;align-items:center;gap:9px;padding:9px 7px;border-radius:8px}.entity-glyph,.test-icon{display:grid;place-items:center;width:27px;height:27px;flex:0 0 27px;color:var(--acid);border:1px solid rgba(72,244,119,.24);border-radius:7px;background:var(--acid-soft)}.entity-glyph svg,.test-icon svg,.row-arrow svg{width:13px;height:13px}.entity-copy,.test-copy{display:grid;gap:2px;min-width:0;flex:1}.entity-copy strong,.test-copy strong{overflow:hidden;font-size:11px;text-overflow:ellipsis;white-space:nowrap}.entity-copy small,.test-copy small{overflow:hidden;color:var(--muted);font-size:10px;text-overflow:ellipsis;white-space:nowrap}.row-arrow{display:grid;place-items:center;color:var(--quiet)}.row-arrow svg{width:14px}.chain-row{display:grid;grid-template-columns:29px minmax(0,1fr) auto;align-items:center;gap:7px;padding:9px 7px;border-radius:8px}.chain-index{color:var(--violet);font:600 10px/1 var(--mono)}.chain-route{display:flex;align-items:center;gap:4px;min-width:0;overflow:hidden;color:#d8e1da;font:10px/1.2 var(--mono)}.chain-route span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.chain-route i{display:flex;flex:0 0 auto;color:var(--quiet)}.chain-route svg{width:11px;height:11px}.chain-proof,.test-confidence{color:var(--acid);font:9px/1 var(--mono);white-space:nowrap}.test-row{display:flex;align-items:center;gap:9px;padding:9px 7px;border-radius:8px}.test-icon{color:var(--violet);border-color:rgba(185,153,255,.25);background:var(--violet-soft)}.warning-list{display:grid;gap:5px;margin:0;padding:9px 12px 12px;list-style:none}.warning-list li{display:flex;align-items:flex-start;gap:7px;color:#c6cec8;font-size:11px}.warning-mark{display:grid;place-items:center;flex:0 0 14px;width:14px;height:14px;margin-top:1px;border-radius:50%;background:rgba(255,191,91,.15);color:var(--amber);font:700 10px/1 var(--mono)}.warning-list .quiet{color:var(--muted)}.quiet .warning-mark{background:var(--acid-soft);color:var(--acid)}.empty{margin:0;padding:11px 5px;color:var(--muted);font-size:11px}.map-empty{display:grid;place-content:center;justify-items:center;gap:8px;position:absolute;inset:0;color:var(--muted);font-size:11px;text-align:center}.map-empty span{color:var(--quiet)}.map-empty svg{width:25px;height:25px}.footer{display:flex;align-items:center;gap:7px;margin:12px 2px 0;color:var(--quiet);font:10px/1.4 var(--mono)}.footer:before{content:"";width:5px;height:5px;flex:0 0 auto;border-radius:50%;background:var(--violet)}@media (min-width:560px){main{padding:18px}.hero{padding:19px}.command-deck{max-width:420px}.data-grid{display:grid;grid-template-columns:1.1fr .9fr;gap:10px}.data-grid .section{margin-top:0}.impact-map{min-height:240px}}@media (max-width:320px){main{padding:10px}.metrics{grid-template-columns:1fr}.meter-labels{font-size:8px}.command-deck{gap:6px}.button{padding:8px}.project-status{align-items:flex-start;flex-direction:column}.chain-row{grid-template-columns:25px minmax(0,1fr)}}@media (prefers-reduced-motion:reduce){*,*:before,*:after{scroll-behavior:auto!important;transition-duration:.01ms!important;animation-duration:.01ms!important}}
</style></head><body><main>
  <header class="topbar"><div class="brand"><span class="brand-mark">${icon("spark")}</span><div><h1 class="brand-title">CodeSlicer</h1><span class="brand-sub">Impact intelligence</span></div></div><span class="signal ${signalTone}"><i class="signal-dot"></i>${runtimeState}</span></header>
  <section class="hero" aria-labelledby="change-title"><p class="eyebrow">LOCAL DIFF <span>→</span> REVIEW</p><h2 id="change-title">Know the consequence<br>before the merge.</h2><p>Evidence-gated impact review for the changes in this workspace.</p><div class="diff-note">${icon("terminal")}<span>${esc(review.localDiffNotice)}</span></div></section>
  <section class="metrics" aria-label="Review signals"><div class="metric"><span class="metric-label">RISK SIGNAL</span><strong class="metric-value ${riskTone(review.riskLevel)}">${esc(riskLabel(review.riskLevel))}</strong><span class="metric-detail"><b>${esc(review.riskConfidence === "—" ? "NO SCORE YET" : review.riskConfidence.toUpperCase())}</b> evidence-gated confidence</span></div><div class="metric"><span class="metric-label">PROOF COVERAGE</span><strong class="metric-value">${proofCoverage}</strong><span class="metric-detail"><b class="violet">${evidenceCount}</b> evidence locations in this slice</span></div></section>
  <div class="meter" aria-label="Evidence distribution"><span class="proof"></span><span class="fresh"></span><span class="fallback"></span></div><div class="meter-labels"><span><i class="key"></i>Proof ${proofCoverage}</span><span><i class="key violet"></i>${project.graphStatus === "present" ? "Graph ready" : "Graph pending"}</span><span><i class="key gray"></i>${review.tests.length} tests</span></div>
  <div class="command-deck"><button type="button" class="button primary" data-action="review">${icon("play")}Review current changes</button><button type="button" class="button icon-only" data-action="refresh" aria-label="Refresh CodeSlicer status" title="Refresh">${icon("refresh")}</button><button type="button" class="button icon-only" data-action="configure" aria-label="Configure CodeSlicer executable" title="Configure executable">${icon("settings")}</button></div>
  <section class="section"><div class="section-heading"><h2>Workspace signals</h2><span class="section-count">${esc(graphState)}</span></div><div class="project-line"><span class="project-path" title="${esc(project.workspace || "No workspace folder")}">${esc(project.workspace || "No workspace folder")}</span><div class="project-status"><span>Canonical graph · ${esc(project.freshness)}</span><b class="${project.graphStatus === "missing" ? "missing" : ""}">${esc(graphState)}</b></div></div><div class="runtime-line">${icon("terminal")}<div class="runtime-copy"><strong>${esc(runtime.status)} · ${esc(runtime.version)}</strong><small title="${esc(runtime.executable || runtime.diagnostic)}">${esc(runtime.executable || runtime.diagnostic)}</small></div></div><div class="list"><button type="button" class="button" data-action="analyze">${icon("spark")}Analyze workspace</button> <button type="button" class="button" data-action="hub">${icon("map")}Open Local Hub</button>${project.graphifyAvailable ? ` <button type="button" class="button" data-action="graphify">${icon("map")}Graphify</button>` : ""}</div></section>
  <section class="section"><div class="section-heading"><h2>Impact topology</h2><span class="section-meta">bounded, confirmed slice</span></div><div class="map-shell"><div class="impact-map" role="group" aria-label="Bounded impact mini-map"><svg class="map-path" viewBox="0 0 100 100" preserveAspectRatio="none" aria-hidden="true"><path d="M15 29 C35 26 35 46 45 50 S67 18 75 19"/><path class="emphasis" d="M14 76 C29 69 32 54 43 51 S61 66 74 67"/><path d="M48 85 C50 69 55 48 74 20"/></svg>${nodes}</div><div class="map-caption"><span>Click a node to inspect provenance.</span><span class="legend"><span><i></i>entity</span><span><i class="l-violet"></i>route</span><span><i class="l-amber"></i>test</span></span></div></div><div class="details" id="details" role="status" aria-live="polite">Select an impact node to reveal its evidence, provenance, file, and line.</div></section>
  <div class="data-grid"><section class="section"><div class="section-heading"><h2>Affected entities</h2><span class="section-count">${review.impacts.length}</span></div><div class="list">${impacts}</div></section><section class="section"><div class="section-heading"><h2>Evidence chains</h2><span class="section-count">${review.chains.length}</span></div><div class="list">${chains}</div></section></div>
  <section class="section"><div class="section-heading"><h2>Targeted tests</h2><span class="section-count">${review.tests.length} READY</span></div><div class="list">${tests}</div></section>
  <section class="section"><div class="section-heading"><h2>Review limits</h2><span class="section-meta">always explicit</span></div><ul class="warning-list">${warnings}</ul></section>
  <footer class="footer">Canonical CodeSlicer evidence only. ${graphifyStatus} Graphify and overlays remain separate optional engines.</footer>
</main><script>
const vscode=acquireVsCodeApi();
document.addEventListener("click",event=>{const button=event.target.closest("button");if(!button)return;const data=button.dataset;if(data.action){vscode.postMessage({type:"action",action:data.action});return}if(data.test!==undefined){vscode.postMessage({type:"runTest",index:Number(data.test)});return}if(data.chain!==undefined){vscode.postMessage({type:"chain",index:Number(data.chain)});return}if(data.entity){document.getElementById("details").textContent="Loading evidence for "+data.entity;vscode.postMessage({type:"entity",entity:data.entity,file:data.file,line:Number(data.line)||undefined})}});
window.addEventListener("message",event=>{if(event.data.type==="details")document.getElementById("details").textContent=event.data.text});
</script></body></html>`;
}
